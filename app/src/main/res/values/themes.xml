<resources>
    <!-- Base application theme. -->
    <style name="Theme.Calculator" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/status_bar_color</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="main_editText_style" parent="ThemeOverlay.Material3.TextInputEditText">
        <item name="android:paddingHorizontal">@dimen/normal_padding</item>
        <item name="android:paddingVertical">@dimen/medium_padding</item>
        <item name="android:textSize">@dimen/fill_text_size</item>
        <item name="android:gravity">center</item>
        <item name="android:inputType">none</item>
        <item name="android:singleLine">true</item>
        <item name="android:scrollHorizontally">true</item>
        <item name="android:background">@drawable/edit_text_selector</item>
        <item name="android:autofillHints" />
    </style>

    <style name="main_editText_style.editText_style">
        <item name="android:layout_marginTop">16dp</item>
    </style>
</resources>